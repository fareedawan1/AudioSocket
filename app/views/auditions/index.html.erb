<div style="text-align:end" class="bg-white pb-2 pt-2">
  <%= link_to "Logout",destroy_user_session_path, method: :delete, class: "nav-link" %></a>
</div>  
<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "All",  auditions_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Pending",  auditions_path(status: 'pending'), class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Accepted",  auditions_path(status: 'accepted'), class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Rejected",  auditions_path(status: 'rejected'), class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Deleted",  auditions_path(status: 'deleted'), class: "nav-link" %>
        </li>
      </ul>
      <%= form_tag auditions_path, method: :get do %>
        <%= search_field_tag :search, params[:search], placeholder: 'Search by Name,Artist and Email' %>
        <%= submit_tag "search", class: 'd-none' %>
      <% end %>
    </div>
  </div>
</nav>
  <div style="text-align:end">
    <%= link_to "Export CSV", "#{auditions_url}.csv?status=#{params[:status]}", class:"btn btn-dark" %>
  </div>
<table class="table bg-light ">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col"><%= sortable " first_name ", "Name" %></th>
      <th scope="col"><%= sortable " artist_name " %></th>
      <th scope="col"><%= sortable " email " %></th>
      <th scope="col"><%= sortable " genre " %></th>
      <th scope="col"><%= sortable " created_at ", "Submitted" %></th>
      <th scope="col">Assigned To</th>
      <th scope="col"><%= sortable " status " %></th>
    </tr>
  </thead>
  <tbody id="audition-show">
    <% @auditions.each do |audition| %>
        <tr  class='clickable-row' data-url="<%= audition_path(audition, id: audition.id, format: :js) %>">
          <td><input type="checkbox" name="brand"> <%= audition.id %> </td>
          <td><%= audition.first_name %> <%= audition.last_name %></td>
          <td><%= audition.artist_name %></td>
          <td><%= audition.email %></td>
          <td><%= audition.genre %></td>
          <td><%= audition.created_at %></td>
          <td>William</td>
          <td><%= audition.status %></td>
        </tr>
    <% end %>
  </tbody>
</table>
<div id="show-audition"></div>
